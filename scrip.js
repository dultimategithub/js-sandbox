var operator = prompt("please, enter the operation to perform: ");

var firstNumber = Number(prompt("Enter the first number: "));
var firstNumber = Number(prompt("Enter the second number: "));

if (operator==="add" || operator==="Add")
{
    var sum = firstNumber + secondNumber;
    alert ("Sum of two numbers is: " + sum);
}

else if (operator==="subtract" || operator==="subtract")
{
    var sub = firstNumber + secondNumber;
    alert ("Sub of two numbers is: " - subtract);
}

else if (operator==="multiplication" || operator==="multiplication")
{
    var product = firstNumber * secondNumber;
    alert ("Product of two numbers is: " * multiplication);
}

else if (operator==="multiplication" || operator==="multiplication")
{
    var div = firstNumber / secondNumber;
    alert ("Div of two numbers is: " / division);
}

else
{
    alert ("invalid input");
}